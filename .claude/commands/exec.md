---
description: Executa o plano aprovado via subagent exec
---

Use o subagent **exec** para implementar o checklist do `tasks/<slug>.md` (Gate Plan = Livre). O agent segue a ordem: tokens → shadcn → componentes → integração → animação → imagens → SEO → validação → build.

Slug ou contexto adicional: $ARGUMENTS
