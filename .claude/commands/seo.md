---
description: Otimiza metadata, OG, sitemap, robots, JSON-LD via subagent seo
---

Use o subagent **seo** para auditar e implementar SEO técnico da landing — `metadata` por rota, OG image, sitemap, robots, JSON-LD `Person`/`Athlete`, e Core Web Vitals.

Foco / pedido: $ARGUMENTS
