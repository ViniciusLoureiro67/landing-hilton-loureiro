---
description: Abre Pull Request para main via subagent pr
---

Use o subagent **pr** para abrir um Pull Request para `main` com o template do projeto, vinculação a issues (`Closes #N`) e link de Preview da Vercel.

Argumentos do usuário (opcional, ex: número de issue ou título do PR): $ARGUMENTS
